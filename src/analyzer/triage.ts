/**
 * Phase C: LLM Crash Triage.
 * Full implementation in next commit — this is the module stub.
 */
import type { CrashFinding } from '../types/index.js';
import type { LLMConfig } from '../reporter/llm.js';

export async function triageCrashes(
  _crashes: CrashFinding[],
  _config: LLMConfig,
): Promise<void> {
  // Placeholder — real implementation coming in feat(analyzer): LLM crash triage
}
